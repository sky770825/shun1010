# 版本說明

## 📋 版本區分

### 舊版本（傳統 HTML/JS）
- **檔案位置**：`index.html`
- **訪問路徑**：`https://shun1010.pages.dev/` 或 `https://shun1010.pages.dev/index.html`
- **技術棧**：純 HTML + JavaScript + CSS
- **特點**：
  - 單一 HTML 檔案包含所有功能
  - 使用 `script.js` 和 `styles.css`
  - 支援 Google Sheets 同步
  - 包含完整的排班表和鑰匙管理功能

### 新版本（React/Vite）
- **檔案位置**：`index1.html`（源檔案）、`dist/index1.html`（建置後）
- **訪問路徑**：`https://shun1010.pages.dev/index1.html` 或 `https://shun1010.pages.dev/index1`
- **技術棧**：React + TypeScript + Vite + Tailwind CSS + shadcn/ui
- **特點**：
  - 現代化 React 應用
  - 組件化架構
  - 整合 Supabase 資料持久化
  - 優化的排班演算法
  - 內嵌式表單（無需彈窗）
  - 底部導航：排班、值班台、工具

## 🗂️ 檔案結構

```
常順班表/
├── index.html          # 舊版本入口（傳統 HTML）
├── index1.html         # React 版本入口（開發用）
├── script.js           # 舊版本 JavaScript
├── styles.css          # 舊版本樣式
├── src/                # React 版本源碼
│   ├── App.tsx
│   ├── main.tsx
│   ├── components/
│   ├── services/
│   └── ...
├── dist/               # React 版本建置輸出
│   ├── index.html      # 舊版本（部署用）
│   ├── index1.html     # React 版本（部署用）
│   └── assets/
└── vite.config.ts      # Vite 配置（使用 index1.html 作為入口）
```

## 🚀 建置與部署

### 建置 React 版本
```bash
cd 常順班表
npm run build
```

### 部署到 Cloudflare Pages
```bash
# 建置後，dist/ 資料夾包含：
# - index.html（舊版本）
# - index1.html（React 版本）
# - assets/（React 版本資源）

npx wrangler pages deploy dist --project-name=shun1010
```

## 📝 注意事項

1. **舊版本**：直接使用 `index.html`，不需要建置
2. **React 版本**：需要執行 `npm run build` 後才能使用
3. **部署時**：兩個版本都會部署，通過不同路徑訪問
4. **開發時**：React 版本使用 `npm run dev`，會自動使用 `index1.html` 作為入口
